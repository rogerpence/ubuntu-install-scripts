hello 
world
goodbye

prompt_end() {
    if [[ -n  ]]; then
        print -n "%{%k%F{}%}"
    else
        print -n "%{%k%}"
    fi

    print -n "%{%f%}"
    CURRENT_BG='' 

    #Adds the new line and ➜ as the start character.
    printf "\n ➜";
}
